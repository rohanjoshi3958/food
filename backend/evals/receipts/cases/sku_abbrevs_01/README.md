# sku_abbrevs_01 (placeholder)

Needs `receipt.jpg`: a Walmart/Kroger receipt with dense SKU abbreviations
(`GV WHL MLK`, `GRND BF`, `STRWBRY`), UPC codes and trailing tax flags on a
single line per item. This is the case that measures `ingredient_name`
expansion quality, so label expansions the way a shopper would say them.
