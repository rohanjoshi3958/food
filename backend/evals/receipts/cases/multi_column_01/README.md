# multi_column_01 (placeholder)

Needs `receipt.jpg`: a wide-format receipt where Tesseract emits the item
column and the price column as separate lines (common with `--psm 4` on
receipts wider than ~3 inches). Exercises the parser's "name then price-only
line" recovery.
